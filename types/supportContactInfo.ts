import { LucideIcon } from "lucide-react";

export interface SupportContactInfo {
  id: number;
  title: string;
  description: string;
  value: string;
  icon: LucideIcon;
  iconBg: string;
  iconColor: string;
}